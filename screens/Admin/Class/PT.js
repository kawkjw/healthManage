import React from "react";
import { Text, SafeAreaView } from "react-native";

export default PT = ({ navigation }) => {
    return (
        <SafeAreaView>
            <Text>PT</Text>
        </SafeAreaView>
    );
};
