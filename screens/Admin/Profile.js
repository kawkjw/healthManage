import React from "react";
import { SafeAreaView, Text } from "react-native";

export default Profile = () => {
    return (
        <SafeAreaView>
            <Text>Profile</Text>
        </SafeAreaView>
    );
};
