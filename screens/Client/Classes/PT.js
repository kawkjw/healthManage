import React from "react";
import { SafeAreaView, Text } from "react-native";

export default PT = ({ navigation }) => {
    return (
        <SafeAreaView>
            <Text>PT</Text>
        </SafeAreaView>
    );
};
